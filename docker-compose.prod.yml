
version: '3.3'
services:
  api:
    image: mcr.microsoft.com/playwright:focal
    container_name: api
    # restart: unless-stopped
    env_file: ./.env
    working_dir: /home/node
    command: >
      /bin/bash -c "npm install --global pnpm@latest-6 && pnpm install -g typescript && cd ./helpers && pnpm install && cd ../apps/api/ && pnpm install && pnpm build && pnpm start"
    volumes:
      - ./src/apps/api:/home/node/apps/api
      - ./src/interfaces:/home/node/interfaces
      - ./src/helpers:/home/node/helpers
    networks:
      - app-network
  bot:
    image: node:16
    container_name: bot
    # restart: unless-stopped
    env_file: ./.env
    working_dir: /home/node
    command: >
      /bin/bash -c "npm install --global pnpm@latest-6 && pnpm install -g typescript && cd ./helpers && pnpm install && cd ../apps/bot/ && pnpm install && pnpm build && pnpm start"
    volumes:
      - ./src/apps/bot:/home/node/apps/bot
      - ./src/interfaces:/home/node/interfaces
      - ./src/helpers:/home/node/helpers
    depends_on:
      - api
    networks:
      - app-network
  web:
    image: node:16
    container_name: web
    # restart: unless-stopped
    ports:
      - 80:3000
    env_file: ./.env
    working_dir: /home/node
    command: >
      /bin/bash -c "npm install --global pnpm@latest-6 && pnpm install -g typescript && cd ./helpers && pnpm install && cd ../apps/web/ && pnpm install && pnpm build && pnpm preview"
    tty: true
    volumes:
      - ./src/apps/web:/home/node/apps/web
      - ./src/interfaces:/home/node/interfaces
      - ./src/helpers:/home/node/helpers
    depends_on:
      - api
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
